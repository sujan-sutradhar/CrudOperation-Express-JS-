<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Crud Operation</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <div class="w-full h-screen bg-zinc-900 p-10">
        <a  class="text-blue-500" href="/">Back to home</a>
        <h1 class="text-3xl text-white mb-3 tracking-tighter">All User</h1>
        
            <div class="users flex flex-wrap gap-5">
                <%users.forEach((user)=>{%>
                <div class="user w-72 p-4 bg-zinc-800">
                     <div class="w-full h-52 bg-zinc-500">
                        <img class="w-full h-full object-top"src="<%=user.image%>" alt="">
                     </div>
                     <h3 class="text-2xl tracking-tighter text-white"><%=user.name%></h3>
                     <h3 class="text-white "><%=user.email%></h3>
                     <div class="mt-2">
                        <a class="text-blue-500 text-2xl" href="/edit/<%=user._id%>">Edit</a>
                        <a class="text-red-500 text-2xl" href="/delete/<%=user._id%>">Delete</a>
                     </div>
                </div>
                <% })%>
            </div>
      
        
    </div>
</body>
</html>